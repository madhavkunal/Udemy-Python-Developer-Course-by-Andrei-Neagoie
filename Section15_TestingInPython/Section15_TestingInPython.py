# pip is the standard package manager for Python - allows us to install, update and remove Python modules, packages and libraries from the Python Standard Library (Python Native Modules) and from PyPI (3rd Party Modules)

# python -m pip install --upgrade pip
# tells python to upgrade/update the pip module

# python3 -m pip install --upgrade package_name
# tells python to install the most recently updated module or upgrade/update any installed modules pre-existing on your system

# python3 -m pip install pytest
# python3 -m pip install --user pytest
# installs pytest - a module that tests python code
